/** Theme barrel — import design tokens and type presets from one place. */
export { colors, fonts, spacing, radius, shadow } from './tokens';
export type { ColorName } from './tokens';
export { textStyles } from './typography';
export type { TypeName } from './typography';
