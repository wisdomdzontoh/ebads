# Alembic configuration (docs/11-development-setup.md §4).
# The database URL is NOT set here; env.py reads it from app.config.Settings so there is a
# single source for the connection string (the DATABASE_URL environment variable).

[alembic]
script_location = app/db/migrations
prepend_sys_path = .
# Use a timestamp prefix so migration files sort chronologically.
file_template = %%(year)d%%(month).2d%%(day).2d_%%(rev)s_%%(slug)s

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
